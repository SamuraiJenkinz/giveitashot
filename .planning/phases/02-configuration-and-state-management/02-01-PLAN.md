---
phase: 02-config-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config.py
  - .env.example
  - tests/test_config.py
autonomous: true

must_haves:
  truths:
    - "MAJOR_UPDATE_TO/CC/BCC are independently configurable from SUMMARY_TO/CC/BCC"
    - "Config.is_major_digest_enabled() returns True when MAJOR_UPDATE_TO has recipients, False when empty"
    - "Config.validate() catches invalid MAJOR_UPDATE_CC/BCC when major digest is enabled"
    - "Config.validate() does NOT raise errors when major digest is not configured"
    - ".env.example has a clearly grouped Major Update Digest section"
  artifacts:
    - path: "src/config.py"
      provides: "MAJOR_UPDATE_TO/CC/BCC fields, is_major_digest_enabled(), validation"
      contains: "MAJOR_UPDATE_TO"
    - path: ".env.example"
      provides: "Major Update Digest config section"
      contains: "MAJOR_UPDATE_TO"
    - path: "tests/test_config.py"
      provides: "Unit tests for major digest config"
      min_lines: 50
  key_links:
    - from: "src/config.py"
      to: "_parse_email_list"
      via: "Reuse existing helper for MAJOR_UPDATE_* fields"
      pattern: "_parse_email_list.*MAJOR_UPDATE"
    - from: "src/config.py"
      to: "Config.validate()"
      via: "Major digest validation in existing validate method"
      pattern: "is_major_digest_enabled"
---

<objective>
Add major update digest recipient configuration to the Config class, with presence-based feature activation and startup validation.

Purpose: Enable dual-digest workflows by allowing separate recipient lists for major update digests (MAJOR_UPDATE_TO/CC/BCC) independent from regular digest recipients (SUMMARY_TO/CC/BCC). This is the config foundation that all later phases depend on.

Output: Extended Config class with major digest fields, feature toggle method, validation, updated .env.example, and unit tests.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/giveitashot/.planning/PROJECT.md
@C:/giveitashot/.planning/ROADMAP.md
@C:/giveitashot/.planning/STATE.md
@C:/giveitashot/.planning/phases/02-configuration-and-state-management/02-CONTEXT.md
@C:/giveitashot/.planning/phases/02-configuration-and-state-management/02-RESEARCH.md
@C:/giveitashot/src/config.py
@C:/giveitashot/.env.example
@C:/giveitashot/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MAJOR_UPDATE_* config fields and feature toggle</name>
  <files>src/config.py, .env.example</files>
  <action>
Extend the Config class in src/config.py to add major update digest recipient configuration. Follow the EXACT existing pattern used for SUMMARY_TO/CC/BCC.

1. Add three new class attributes after the existing SUMMARY_BCC line (around line 59):
   ```python
   # Major Update Digest Recipients (optional, enables major digest when TO has recipients)
   MAJOR_UPDATE_TO: list[str] = _parse_email_list("MAJOR_UPDATE_TO")
   MAJOR_UPDATE_CC: list[str] = _parse_email_list("MAJOR_UPDATE_CC")
   MAJOR_UPDATE_BCC: list[str] = _parse_email_list("MAJOR_UPDATE_BCC")
   ```

2. Add a classmethod `is_major_digest_enabled()` that returns `bool(cls.MAJOR_UPDATE_TO)`. This is the presence-based feature toggle — no separate ENABLE_MAJOR_DIGEST flag needed.

3. Add a classmethod `get_major_recipients()` that returns `cls.MAJOR_UPDATE_TO` (mirrors get_recipients() pattern but with NO fallback — major digest recipients must be explicitly configured).

4. Extend the existing `validate()` method to add major digest validation AFTER the existing regular digest validation:
   - If `is_major_digest_enabled()` is True, validate that MAJOR_UPDATE_CC and MAJOR_UPDATE_BCC entries contain "@" (basic email format check matching existing pattern)
   - If invalid entries found, raise ValueError with descriptive message listing the invalid addresses
   - If major digest is NOT enabled, log at debug level: "Major digest not enabled (MAJOR_UPDATE_TO empty or missing)"
   - If major digest IS enabled, log at info level the number of TO recipients

5. Update .env.example to add a new section after the existing Summary Email Recipients section:
   ```
   # ============================================
   # Major Update Digest Recipients (Optional)
   # ============================================
   # Separate recipients for M365 Message Center major update digests.
   # Major digest activates automatically when MAJOR_UPDATE_TO has recipients.
   # These are fully independent from regular summary recipients above.

   # TO recipients for major update digest (enables feature when set)
   # MAJOR_UPDATE_TO=admin@company.com,it-lead@company.com

   # CC recipients (optional)
   # MAJOR_UPDATE_CC=it-team@company.com

   # BCC recipients (optional)
   # MAJOR_UPDATE_BCC=compliance@company.com
   ```

IMPORTANT: Do NOT add any fallback from MAJOR_UPDATE_TO to SUMMARY_TO. These are intentionally separate audiences. Do NOT add a boolean ENABLE_MAJOR_DIGEST env var.
  </action>
  <verify>
Run `python -c "from src.config import Config; print(Config.MAJOR_UPDATE_TO); print(Config.is_major_digest_enabled())"` and confirm it outputs `[]` then `False` (since .env has no MAJOR_UPDATE_TO set).
  </verify>
  <done>
Config class has MAJOR_UPDATE_TO/CC/BCC fields using _parse_email_list, is_major_digest_enabled() returns bool based on MAJOR_UPDATE_TO presence, get_major_recipients() returns MAJOR_UPDATE_TO list, validate() checks major digest config when enabled, .env.example has Major Update Digest section with all vars commented out.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for major digest configuration</name>
  <files>tests/test_config.py</files>
  <action>
Create tests/test_config.py with pytest tests covering the major digest configuration. Use monkeypatch to set environment variables (follow existing conftest.py patterns).

Tests to write:

1. **test_major_digest_disabled_when_no_recipients** — With no MAJOR_UPDATE_TO set, is_major_digest_enabled() returns False
2. **test_major_digest_enabled_when_recipients_set** — With MAJOR_UPDATE_TO="admin@co.com", is_major_digest_enabled() returns True
3. **test_major_recipients_parsed_correctly** — MAJOR_UPDATE_TO="a@b.com, c@d.com" parses to ["a@b.com", "c@d.com"]
4. **test_major_cc_bcc_parsed** — MAJOR_UPDATE_CC and MAJOR_UPDATE_BCC parse correctly
5. **test_get_major_recipients_returns_to_list** — get_major_recipients() returns MAJOR_UPDATE_TO list
6. **test_get_major_recipients_no_fallback** — When MAJOR_UPDATE_TO empty, get_major_recipients() returns [] (does NOT fall back to SUMMARY_TO)
7. **test_validate_passes_when_major_disabled** — validate() does not raise when MAJOR_UPDATE_TO is empty (major digest simply not enabled)
8. **test_validate_passes_when_major_enabled_valid** — validate() passes with valid MAJOR_UPDATE_TO/CC/BCC
9. **test_validate_fails_invalid_major_cc** — validate() raises ValueError when MAJOR_UPDATE_CC has entries without "@"
10. **test_validate_fails_invalid_major_bcc** — validate() raises ValueError when MAJOR_UPDATE_BCC has entries without "@"

IMPORTANT: Since Config uses class-level attributes loaded at import time from os.getenv, tests need to either:
- Use monkeypatch on os.environ AND reload the config module, OR
- Directly set Config class attributes in test setup and restore in teardown

Check how the existing codebase handles this. If no existing pattern, use direct attribute setting with a fixture that saves/restores original values. This is simpler and avoids module reload complexity.

Follow existing test patterns: descriptive test names, clear arrange-act-assert structure, use pytest.raises for error cases.
  </action>
  <verify>
Run `python -m pytest tests/test_config.py -v` and confirm all 10 tests pass.
  </verify>
  <done>
10 unit tests covering: feature toggle on/off, recipient parsing, no-fallback behavior, validation passes when disabled, validation passes when valid, validation fails on invalid CC/BCC. All tests pass.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from src.config import Config; print(hasattr(Config, 'MAJOR_UPDATE_TO'))"` returns True
2. `python -c "from src.config import Config; print(hasattr(Config, 'is_major_digest_enabled'))"` returns True
3. `python -m pytest tests/test_config.py -v` — all tests pass
4. `python -m pytest tests/ -v` — all existing tests still pass (no regressions)
5. Grep src/config.py for "MAJOR_UPDATE" — fields, toggle method, and validation present
6. Grep .env.example for "MAJOR_UPDATE" — all three vars documented
</verification>

<success_criteria>
- Config.MAJOR_UPDATE_TO/CC/BCC fields exist and parse from environment using _parse_email_list
- Config.is_major_digest_enabled() returns True only when MAJOR_UPDATE_TO is non-empty
- Config.get_major_recipients() returns MAJOR_UPDATE_TO with no fallback
- Config.validate() validates major recipients only when feature is enabled
- .env.example documents all MAJOR_UPDATE_* variables in a grouped section
- 10 unit tests pass covering all config behaviors
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-configuration-and-state-management/02-01-SUMMARY.md`
</output>
