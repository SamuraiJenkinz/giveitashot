---
phase: 05-integration-testing
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - tests/fixtures/real/
  - tests/test_integration_dual_digest.py
autonomous: false

must_haves:
  truths:
    - "Real .eml samples are sanitized (no PII, tenant GUIDs, or real domain names)"
    - "Classification correctly identifies real Message Center major updates"
    - "Full pipeline works with real email data (classify -> extract -> format)"
    - "Dry-run HTML output renders correctly in browser for visual verification"
  artifacts:
    - path: "tests/fixtures/real/"
      provides: "Sanitized real .eml fixtures from production mailbox"
  key_links:
    - from: "tests/test_integration_dual_digest.py"
      to: "tests/fixtures/real/"
      via: "load_eml('real', filename)"
      pattern: "load_eml.*real"
---

<objective>
Integrate real .eml samples provided by user, add real-data integration tests, and visually verify dry-run HTML output.

Purpose: Synthetic fixtures test the code paths, but real Message Center emails validate that detection patterns actually match production email formats. This is the final validation gate before enabling production sends. The user must provide and sanitize real .eml exports.

Output: Sanitized real .eml fixtures, integration tests using real data, visual verification of both digest HTML outputs.
</objective>

<execution_context>
@C:\Users\taylo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\taylo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-integration-testing/05-CONTEXT.md
@.planning/phases/05-integration-testing/05-RESEARCH.md
@.planning/phases/05-integration-testing/05-01-SUMMARY.md
@.planning/phases/05-integration-testing/05-02-SUMMARY.md
@tests/conftest.py
@tests/test_integration_dual_digest.py
@tests/fixtures/README.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User provides real .eml samples</name>
  <action>
    Export 5-7 .eml files from the shared mailbox. The minimum set needed:

    1. **Major update with action-required date** (e.g., MC###### with deadline)
    2. **Major update retirement notice** (RETIREMENT keyword)
    3. **Major update new feature** (new feature announcement with admin action)
    4. **Minor update** (Message Center email that is NOT a major update -- no major keywords)
    5. **Regular non-MC email** (any normal email from the shared mailbox)

    Optional but valuable:
    6. **Updated version of an earlier MC ID** (same MC# as sample 1, newer date -- tests deduplication)
    7. **Major update with word-format date** (e.g., "March 15, 2026" instead of "03/15/2026")

    Place raw exports in `tests/fixtures/real/` directory.

    Then sanitize each file following `tests/fixtures/README.md` checklist:
    - Replace all @mmc.com / @marsh.com emails with @company.com
    - Replace real display names with "Test User", "Test Admin", etc.
    - Replace tenant GUIDs (look for long hex strings in message IDs and headers)
    - Replace internal URLs/domains
    - Verify with: `grep -riE "mmc\.com|marsh\.com|real-name" tests/fixtures/real/`

    The MC content patterns (MC######, "MAJOR UPDATE", "Action required by", "RETIREMENT", affected services) should be PRESERVED -- those are the patterns we're testing against.
  </action>
  <resume-signal>Type "samples ready" when sanitized .eml files are in tests/fixtures/real/</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Add real-data integration tests and run full pipeline</name>
  <files>tests/test_integration_dual_digest.py</files>
  <action>
    After user provides sanitized .eml files, add tests to the existing test_integration_dual_digest.py file:

    **Add to TestEndToEndPipeline class (or create new TestRealEmailIntegration class):**

    1. `test_real_eml_sanitization_check`: For each .eml file in tests/fixtures/real/, load it and assert the content does NOT contain known PII patterns (mmc.com, marsh.com). Parametrize over all files in the directory. This test prevents accidental PII commits.

    2. `test_real_major_update_classification`: Load each real major update .eml, convert to Email object, run through EmailClassifier.classify(). Assert is_major_update=True with confidence_score >= 70. This validates that real Message Center email formats match our detection patterns.

    3. `test_real_minor_update_not_classified_as_major`: Load the real minor update .eml. Classify. Assert is_major_update=False (confidence below threshold).

    4. `test_real_regular_email_not_classified_as_major`: Load the real regular email .eml. Classify. Assert is_major_update=False.

    5. `test_real_major_updates_full_pipeline`: Load all real major update .eml files. Run through full pipeline: classify -> extract fields -> deduplicate -> format HTML. Assert:
       - MC IDs extracted correctly (non-None)
       - Action dates extracted where present
       - HTML output is non-empty and contains MC IDs
       - Urgency tiers assigned based on date proximity

    6. `test_real_emails_mixed_batch_classification`: Load ALL real .eml files (major, minor, regular). Run classify_batch. Verify correct split counts match expected classification.

    All real .eml tests should use `pytest.mark.skipif` or the load_eml fixture's `pytest.skip()` behavior -- if real fixtures aren't present, tests skip gracefully (this allows CI to run without real samples).

    After tests pass, run the full test suite to confirm no regressions.
  </action>
  <verify>
    - `python -m pytest tests/test_integration_dual_digest.py -v -k "real"` -- real data tests pass (or skip if no fixtures)
    - `python -m pytest` -- full suite passes
    - `grep -riE "mmc\.com|marsh\.com" tests/fixtures/real/` -- returns empty (no PII)
  </verify>
  <done>Real .eml fixtures are sanitized and committed. Integration tests validate classification accuracy against real Message Center email formats. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of dry-run HTML output</name>
  <what-built>
    Complete dual-digest system with:
    - Integration tests passing (synthetic + real fixtures)
    - Dry-run HTML preview saving to output/ with browser auto-open
    - Full pipeline: classify -> extract -> format for both digest types
  </what-built>
  <how-to-verify>
    1. Run: `python -m src.main --dry-run --debug`
       (This requires the .env to be configured with mailbox access)

    2. Check console output:
       - Should show regular digest preview (recipients, subject, email count)
       - Should show major digest preview (recipients, subject, MC IDs, urgency breakdown)
       - Should print file paths for saved HTML files

    3. Verify HTML files opened in browser:
       - `output/regular_digest.html` -- regular digest format, existing look
       - `output/major_digest.html` -- major digest format with gradient header, urgency sections, color-coded borders, AI action items (if Azure OpenAI configured)

    4. Visual check on major digest HTML:
       - MC IDs visible for each update
       - Urgency color-coding correct (red=Critical, amber=High, green=Normal)
       - Action-required dates shown with countdown
       - Affected services and categories displayed
       - Professional layout with inline styles

    5. Confirm: `python -m pytest` shows all tests passing (existing + new)
  </how-to-verify>
  <resume-signal>Type "approved" if both digests look correct, or describe any visual/functional issues</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest` -- all tests pass (existing 131 + all new integration + dry-run tests)
- `grep -riE "mmc\.com|marsh\.com" tests/fixtures/real/` -- no PII in committed fixtures
- `python -m src.main --dry-run` -- both HTML files generated and opened in browser
- Real .eml samples correctly classified by the detection system
</verification>

<success_criteria>
- 5-7 sanitized real .eml fixtures committed to tests/fixtures/real/
- Real data tests validate classification accuracy (major updates detected, regular emails excluded)
- PII sanitization verified (no real domains, names, or GUIDs in committed fixtures)
- Dry-run HTML output visually verified by user for both digest types
- Full test suite passes with zero regressions
- System ready for manual dry-run against live mailbox (final production gate)
</success_criteria>

<output>
After completion, create `.planning/phases/05-integration-testing/05-03-SUMMARY.md`
</output>
